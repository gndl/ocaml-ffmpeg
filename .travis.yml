language: c
dist: xenial
sudo: false
addons:
  apt:
    packages:
    - ocaml-nox
    - ocaml-findlib
    - libavutil-dev
    - libavformat-dev
    - libavcodec-dev
    - libswscale-dev
    - libswresample-dev

script: ./bootstrap && ./configure && make
